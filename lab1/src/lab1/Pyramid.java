package lab1;

public class Pyramid {

	public static void main(String[] args) {
		String space = " ";
		String asterisk = "*";
		
		if (args.length == 1) {
			try {
		        int height = Integer.parseInt(args[0]);
		    } catch (NumberFormatException e) {
		        System.err.println("Argument" + " must be an integer");
		        System.exit(1);
			for (int i = 0, i < n, i++);
				System.out.println();
					
		}
		
		if (args.length == 0) {
			System.out.println("Please include an integer argument!");
			System.exit(1);
			
		} if (args.length > 1){
			System.out.println("You've supplied too many arguments! Please only use 1.");
			System.exit(1);
			
		
		}
		
	}

}
